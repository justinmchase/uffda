# Pipe matches Then steps joined by `|>` as a Pipeline AST.
# Uses std one for length-1 collapse (B4) with key `steps`.
import "./then.uff" Then;

export Pipe;

rule PipeTail =
  "|" ">" t:Then -> t;

rule Pipe =
  Then
  PipeTail*
  -> (one (flat _) "pipeline" "steps");
