# Reference matches a string Identifier and projects a ReferenceExpression.
import "../common/identifier.uff" IdToken;

export Reference;

rule Reference =
  IdToken
  -> { kind: "reference", name: _ };
